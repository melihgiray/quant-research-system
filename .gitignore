# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Virtual envs
.venv/
venv/
env/

# Data cache + generated reports (regenerated by the CLI)
data_cache/
reports/*.png

# OS / editor
.DS_Store
.idea/
.vscode/

# Secrets — never commit API keys / tokens
.env
.env.local
.coverage.json

# Large local options-data samples (OptionsDX). Not redistributable here; the
# committed test fixture is a tiny hand-built schema clone. See README.
/data_cache/options/
spy_sample*.csv
!tests/fixtures/optionsdx_tiny.csv
.coverage
