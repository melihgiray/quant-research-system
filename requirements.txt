# ── Core (required) ───────────────────────────────────────────────────────────
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
yfinance>=0.2
pyarrow>=14            # Parquet price cache
requests>=2.28

# ── Regime + ML extras (recommended) ──────────────────────────────────────────
hmmlearn>=0.3         # 2-state Gaussian HMM regime detector
shap>=0.43            # Shapley feature attribution for the ML signal

# ── Monitors (optional) ───────────────────────────────────────────────────────
anthropic>=0.100      # LLM market analyst (structured output + prompt caching)
twilio>=8             # SMS alerts for the EDGAR dilution monitor

# ── Optional niceties ─────────────────────────────────────────────────────────
pandas-datareader>=0.10   # 3-month T-bill risk-free rate from FRED
# lightgbm>=4             # alternative GBM backend; on macOS needs:  brew install libomp

# ── Dev ───────────────────────────────────────────────────────────────────────
pytest>=7
